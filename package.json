{
  "name": "my-library-app",
  "private": true,
  "scripts": {
    "api": "cd api && npm run dev",
    "app": "cd app && npm run dev",
    "dev": "npm-run-all --parallel api app"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
